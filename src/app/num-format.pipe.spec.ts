import { NumFormatPipe } from './num-format.pipe';

describe('NumFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NumFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
